# SQL Business Analytics   

📊 This project demonstrates how SQL can be used for **business analytics** across industries such as e-commerce, retail, or fintech.  
It answers key business questions using structured query language (SQL), showcasing skills in data cleaning, querying, and deriving insights.  

---

## 📂 Dataset  
- **Source**: Public dataset from Kaggle (E-commerce Sales / Retail Sales). 
- **Sample Columns**:  
  - `order_id`  
  - `customer_id`  
  - `region`  
  - `product_category`  
  - `order_date`  
  - `quantity`  
  - `unit_price`  
  - `payment_method`  

---

## 🔑 Business Questions Answered  

1. **Revenue Analysis**  
   - What is the total revenue generated by each product category?  
   - Which regions contribute the most revenue?  

2. **Customer Analytics**  
   - Who are the top customers by lifetime value?  
   - What is the repeat purchase rate?  

3. **Trend Analysis**  
   - Monthly/quarterly revenue trend.  
   - Seasonal sales patterns.  

4. **Operational Metrics**  
   - Average Order Value (AOV).  
   - Distribution of payment methods.  

---

## 🛠️ SQL Concepts Used  
- Aggregations (`SUM`, `COUNT`, `AVG`)  
- Joins  
- Subqueries & CTEs  
- Window Functions (RANK, ROW_NUMBER)  
- Date Functions  

---

## 📌 Example Queries  

```sql
-- Total revenue by region
SELECT region, 
       SUM(quantity * unit_price) AS total_revenue
FROM company_sales
GROUP BY region
ORDER BY total_revenue DESC;

-- Top 10 customers by lifetime value
SELECT customer_id, 
       SUM(quantity * unit_price) AS lifetime_value
FROM company_sales
GROUP BY customer_id
ORDER BY lifetime_value DESC
LIMIT 10;
```

## 🚀 How to Run
- Import company_sales.csv into your SQL database (PostgreSQL/MySQL/SQLite).
- Run queries in /queries/.
- Explore results in /results/.

## 📈 Business Impact

- The queries and insights can help companies:
- Identify high-value customers for loyalty campaigns.
- Optimize product inventory across regions.
- Track revenue performance and uncover seasonal trends.
- Improve decision-making with data-driven insights.
